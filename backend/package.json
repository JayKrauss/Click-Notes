{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "lint": "eslint **/*.js --quiet",
    "lint-all": "eslint **/*.js",
    "lint-ci": "eslint '**/*.js' --quiet"
  },
  "eslintConfig": {
    "extends": "eslint:recommended"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "eslint": "^5.16.0"
  }
}
